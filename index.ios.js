import React from 'react'
import { AppRegistry } from 'react-native'
import AppRootContainer from './src/AppRootContainer'

const ElecApp = () => (
  <AppRootContainer />
)

AppRegistry.registerComponent('ElecApp', () => ElecApp);
